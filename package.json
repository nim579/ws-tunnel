{
  "name": "ws-tunnel",
  "version": "0.2.1",
  "description": "WebSocket tunnel from local machine to your global server.",
  "main": "index.js",
  "dependencies": {
    "commander": "^2.8.1",
    "request": "^2.64.0",
    "underscore": "^1.8.3",
    "ws": "^0.8.0"
  },
  "devDependencies": {
    "coffee-script": "^1.10.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf lib/ && exec $(npm bin)/coffee -c -o lib/ src/",
    "client": "./bin/client",
    "server": "./bin/server"
  },
  "bin": {
    "ws-tunnel": "bin/client",
    "ws-tunnel-server": "bin/server"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/nim579/ws-tunnel.git"
  },
  "author": "Nick Iv",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/nim579/ws-tunnel/issues"
  },
  "homepage": "https://github.com/nim579/ws-tunnel"
}
